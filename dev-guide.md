# ---

**🏨 酒店管理系统 (Yisu Hotel) \- 开发协作指南**

欢迎加入开发团队！为了保证代码质量和风格统一，本项目配置了自动化规范工具（Husky \+ Commitlint \+ Prettier）。请务必阅读以下流程。

## **1\. 快速上手 (Setup)**

### **第一步：获取代码**

Bash

\# 1\. 克隆仓库 (请找 Leader 获取仓库地址)  
git clone https://github.com/564603826/yisu-hotel-project.git

\# 2\. 进入目录  
cd yisu-hotel-project

### **第二步：安装依赖 (至关重要 ⚠️)**

本项目使用了 Husky 进行提交拦截，**必须安装依赖才能生效**，否则提交会报错。

Bash

\# 在根目录下运行  
npm install

## ---

**2\. 分支管理规范 (Branching)**

我们采用 **Trunk Based** 开发模式，所有分支基于 master 创建。

### **命名格式**

type/scope-description

| 组成部分        | 说明                              | 示例       |
| :-------------- | :-------------------------------- | :--------- |
| **type**        | 分支类型 (feat, fix)              | feat       |
| **scope**       | 涉及的子项目 (pc, server, mobile) | pc         |
| **description** | 功能简述 (用中划线连接)           | login-page |

### **示例**

- 开发 PC 端登录页：feat/pc-login
- 修复服务端接口 Bug：fix/server-auth-api
- 修改移动端首页样式：style/mobile-home-css

## ---

**3\. 提交说明规范 (Commit Message)**

本项目启用了 **Commitlint** 检查。如果不符合以下格式，git commit **会失败**。

### **标准格式**

Plaintext

type(scope): subject

_(注意：冒号后面必须有一个**空格**)_

### **1\. Type (必填)**

告诉大家这次提交干了什么：

- **feat**: ✨ 新增功能 (Feature)
- **fix**: 🐛 修复 Bug
- **docs**: 📝 仅修改文档
- **style**: 💄 修改格式（不影响代码逻辑，如空格、分号）
- **refactor**: ♻️ 代码重构（不修 Bug 也不加功能）
- **chore**: 🔧 构建工具或依赖变动 (如配置 husky, package.json)

### **2\. Scope (选填，建议填)**

告诉大家修改了哪个子项目：

- pc \-\> client-pc 项目
- server \-\> 后端项目
- root \-\> 根目录配置

### **3\. Subject (必填)**

简短的描述（建议英文，中文也可）。

### **✅ 正确示例 (可以直接抄)**

Bash

git commit \-m "feat(pc): add hotel list table"  
git commit \-m "fix(server): resolve login timeout issue"  
git commit \-m "chore: update prettier config"  
git commit \-m "docs: update readme"

### **❌ 错误示例 (会被拦截)**

- git commit \-m "update" (没有 type)
- git commit \-m "feat(pc):add login" (冒号后没空格)
- git commit \-m "优化了代码" (没有 type)

## ---

**4\. 开发工作流 (Workflow)**

### **🟢 开始工作前**

每天早上或开发新功能前，务必拉取最新代码，防止冲突。

Bash

git checkout master  
git pull origin master

### **🟡 开发中**

创建你的功能分支：

Bash

git checkout \-b feat/pc-user-management  
\# ... 写代码 ...

### **🔵 提交代码**

Bash

git add .  
git commit \-m "feat(pc): implement user add function"

**注意**：提交时会自动运行 Prettier 格式化代码。如果代码有严重语法错误，提交会被终止，请修复后重试。

### **🟣 推送到远程**

Bash

git push origin feat/pc-user-management

推送成功后，请在 GitHub 上发起 **Pull Request (PR)** 请求合并到 master。

## ---

**5\. 常用 Git 命令速查表**

| 场景                  | 命令                                               |
| :-------------------- | :------------------------------------------------- |
| **查看当前状态**      | git status (最常用的命令！)                        |
| **查看修改内容**      | git diff                                           |
| **暂存所有修改**      | git add .                                          |
| **回退刚才的 commit** | git reset \--soft HEAD^ (保留代码，只撤销提交记录) |
| **丢弃本地修改**      | git checkout . (慎用！代码会找不回来)              |
| **查看提交历史**      | git log \--oneline                                 |
| **删除本地分支**      | git branch \-d \<分支名\>                          |

## ---

**6\. 常见问题 (FAQ)**

**Q: 为什么我提交时报错 husky \- pre-commit hook exited with code 1？**

A: 这说明你的代码格式化失败或者有语法错误。请检查报错信息，修复代码后再提交。

**Q: 为什么报错 subject may not be empty？**

A: 你的提交信息格式不对。请严格按照 type(scope): subject 格式书写。

**Q: 我需要配置 VS Code 的 Prettier 插件吗？**

A: 推荐配置，但不是必须的。只要你运行 git commit，项目会自动帮你格式化代码。

---

_Created by Project Architect_
